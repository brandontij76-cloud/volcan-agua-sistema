// public/js/tema.js
//
// El sitio usa un unico tema visual fijo (fondo oscuro + acentos verdes),
// definido directamente en css/style.css, y ya no cambia segun la hora del
// dia. Este archivo se mantiene (algunas paginas todavia lo referencian)
// pero no hace nada: se conserva solo para no romper el <script> que ya
// esta enlazado en cada HTML.
