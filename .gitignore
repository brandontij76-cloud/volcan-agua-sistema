# Nunca subir credenciales ni secretos reales a git.
.env
.env.local
*.env

# Dependencias (se reinstalan con npm install, no hace falta subirlas)
node_modules/

# Archivos temporales del sistema operativo / editor
.DS_Store
Thumbs.db
*.log
